# OurRoots.org website

## Setup
    npm install -D tailwindcss

## Develop (watch)
    npx tailwindcss -i ./input.css -o ./output.css --watch

## Deploy (minify)
    npx tailwindcss -i ./input.css -o ./output.css --minify
